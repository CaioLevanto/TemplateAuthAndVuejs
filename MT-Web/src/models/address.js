export default class Address {

    constructor(cep, 
                city, 
                state, 
                addressName, 
                numberAddress) {

      this.cep = cep;
      this.city = city;
      this.state = state;
      this.addressName = addressName;
      this.numberAddress = numberAddress;
    }
    
}