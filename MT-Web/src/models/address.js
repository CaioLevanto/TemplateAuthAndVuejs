export default class Address {

    constructor(cep, 
                state, 
                city, 
                zipcode, 
                numberAddress) {

      this.cep = cep;
      this.state = state;
      this.city = city;
      this.zipcode = zipcode;
      this.numberAddress = numberAddress;
    }
    
}